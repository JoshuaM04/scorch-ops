import SearchBar from '../components/SearchBar';

export default function Customers() {
    return (
        <div className="customers-component flex flex-col gap-30 min-h-dvh w-full">
            <SearchBar />
        </div>
    );
}