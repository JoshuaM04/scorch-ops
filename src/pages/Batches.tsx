import SearchBar from '../components/SearchBar';

export default function Batches() {
    return (
        <div className="batches-component flex flex-col gap-30 min-h-dvh w-full">
            <SearchBar />
        </div>
    );
}